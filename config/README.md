# 품목 분류 설정

`product_classification_rules.csv`에서 기업별 우선 분류 기준을 관리합니다.

- `우선순위`: 숫자가 작을수록 먼저 적용
- `사용`: `Y`이면 적용, `N`이면 제외
- `일치방식`: `포함`, `정확히`, `정규식`
- `키워드`: 품목명에서 찾을 표현
- `대분류`, `중분류`: 일치할 때 적용할 기업 카테고리

CSV 규칙은 서비스의 기본 규칙보다 먼저 적용됩니다. 같은 키워드의 분류를
변경하려면 기존 행의 카테고리를 수정하거나 더 높은 우선순위의 행을 추가합니다.
